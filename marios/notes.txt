

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Depth Wavy Dots</title>

<style>

html,body{
margin:0;
padding:0;
height:100%;
overflow:hidden;
background:#0a0a0a;
font-family:sans-serif;
}

/* dot grid container */
.bg{
position:fixed;
inset:0;
display:grid;
grid-template-columns:repeat(auto-fill,minmax(28px,1fr));
grid-auto-rows:28px;
}

/* dot style */
.dot{
width:8px;
height:8px;
border-radius:50%;
background:#ff7a00;
margin:auto;
animation:wave 4s ease-in-out infinite;
opacity:.45;
}

/* blurred depth dots */
.blur{
filter:blur(2px);
opacity:.55;
}

.blur2{
filter:blur(4px);
opacity:.35;
}

/* wave animation */
@keyframes wave{
0%{transform:translateY(0)}
50%{transform:translateY(-14px)}
100%{transform:translateY(0)}
}

.overlay:hover{
background:rgba(0,0,0,.55);
}

.content{
position:relative;
z-index:2;
height:100%;
display:flex;
align-items:center;
justify-content:center;
color:white;
font-size:2rem;
pointer-events:none;
}

</style>
</head>

<body>

<div class="bg" id="dots"></div>
<div class="overlay"></div>

<div class="content">
Wavy Glass Dots Background
</div>

<script>

const container = document.getElementById("dots");

/* create lots of dots so screen always filled */
const totalDots = 1800;

for(let i=0;i<totalDots;i++){

let dot = document.createElement("div");
dot.className = "dot";

/* random wave delay */
dot.style.animationDelay = Math.random()*4+"s";

/* random blur depth */
let r = Math.random();

if(r < 0.25){
dot.classList.add("blur");
}
else if(r < 0.10){
dot.classList.add("blur2");
}

container.appendChild(dot);

}

</script>

</body>
</html>

